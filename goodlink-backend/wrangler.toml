name = "goodlink-backend"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Routes for production environment
[env.production]
routes = ["glynk.to/*"]

# Note: For custom domains, configure them in Cloudflare Dashboard
# Routes are automatically handled when the domain is connected to the worker
#
# IMPORTANT: When deploying, use:
#   npx wrangler deploy --env production
#
# Or configure routes in Cloudflare Dashboard → Workers & Pages → goodlink-backend → Settings → Triggers

